---
// Componente para gestión de imágenes
---

<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-4">
  <h3 class="text-lg font-medium text-gray-900 mb-4">
    <i class="fas fa-images text-green-600 mr-2"></i>
    Gestión de Imágenes
  </h3>
  
  <!-- Tabs -->
  <div class="border-b border-gray-200 mb-4">
    <nav class="flex space-x-8">
      <button
        type="button"
        id="uploadTab"
        onclick="switchImageTab('upload')"
        class="image-tab active py-2 px-1 border-b-2 border-green-500 font-medium text-sm text-green-600"
      >
        Subir Imagen
      </button>
      <button
        type="button"
        id="urlTab"
        onclick="switchImageTab('url')"
        class="image-tab py-2 px-1 border-b-2 border-transparent font-medium text-sm text-gray-500 hover:text-gray-700"
      >
        URL Externa
      </button>
      <button
        type="button"
        id="assetsTab"
        onclick="switchImageTab('assets')"
        class="image-tab py-2 px-1 border-b-2 border-transparent font-medium text-sm text-gray-500 hover:text-gray-700"
      >
        Assets Locales
      </button>
    </nav>
  </div>

  <!-- Upload Panel -->
  <div id="uploadPanel" class="image-panel">
    <div class="border-2 border-dashed border-gray-300 rounded-lg p-6 text-center hover:border-gray-400 transition-colors">
      <input
        type="file"
        id="imageUpload"
        accept="image/*"
        class="hidden"
        onchange="handleImageUpload(event)"
      />
      <label for="imageUpload" class="cursor-pointer">
        <div class="mx-auto h-12 w-12 text-gray-400">
          <i class="fas fa-cloud-upload-alt text-3xl"></i>
        </div>
        <div class="mt-2">
          <p class="text-sm font-medium text-gray-900">Subir imagen</p>
          <p class="text-xs text-gray-500">PNG, JPG, GIF hasta 10MB</p>
        </div>
      </label>
    </div>
    
    <!-- Preview -->
    <div id="imagePreview" class="hidden mt-4">
      <div class="bg-gray-50 rounded-lg p-4">
        <div class="flex items-center justify-between mb-2">
          <span class="text-sm font-medium text-gray-900">Vista previa:</span>
          <button
            type="button"
            onclick="clearImagePreview()"
            class="text-red-600 hover:text-red-800 text-sm"
          >
            <i class="fas fa-times"></i> Quitar
          </button>
        </div>
        <img id="previewImg" class="max-w-full h-32 object-cover rounded" />
        <div class="mt-2 flex space-x-2">
          <button
            type="button"
            onclick="insertImageMarkdown('preview')"
            class="px-3 py-1 bg-green-600 text-white text-xs rounded hover:bg-green-700"
          >
            Insertar en Blog
          </button>
          <button
            type="button"
            onclick="copyImageUrl('preview')"
            class="px-3 py-1 bg-blue-600 text-white text-xs rounded hover:bg-blue-700"
          >
            Copiar URL
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- URL Panel -->
  <div id="urlPanel" class="image-panel hidden">
    <div class="space-y-3">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">URL de la imagen:</label>
        <input
          type="url"
          id="imageUrl"
          placeholder="https://ejemplo.com/imagen.jpg"
          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500"
        />
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Texto alternativo:</label>
        <input
          type="text"
          id="imageAlt"
          placeholder="Descripción de la imagen"
          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500"
        />
      </div>
      <button
        type="button"
        onclick="insertImageFromUrl()"
        class="px-4 py-2 bg-green-600 text-white text-sm rounded hover:bg-green-700"
      >
        Insertar Imagen
      </button>
    </div>
  </div>

  <!-- Assets Panel -->
  <div id="assetsPanel" class="image-panel hidden">
    <div class="text-sm text-gray-600 mb-3">
      <i class="fas fa-info-circle text-blue-500 mr-1"></i>
      Selecciona una imagen de tus assets locales:
    </div>
    
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3" id="assetsGrid">
      <!-- Se llenará con JavaScript -->
    </div>
    
    <button
      type="button"
      onclick="loadAssets()"
      class="mt-3 px-4 py-2 bg-blue-600 text-white text-sm rounded hover:bg-blue-700"
    >
      <i class="fas fa-refresh mr-1"></i>
      Actualizar Assets
    </button>
  </div>
</div>

<script>
// Las funciones se definen en el archivo principal de blogs
</script>
